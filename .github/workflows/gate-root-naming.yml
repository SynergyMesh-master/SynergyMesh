name: 🚪 Root Naming Policy Gate

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]
    paths:
      - root.*
      - init.d/**
      - bin/**
      - sbin/**
      - etc/**
      - lib/**
      - var/**
      - usr/**

permissions:
  contents: read
  pull-requests: write

jobs:
  validate_root_naming:
    name: 🔍 驗證 root/ 命名規範
    runs-on: ubuntu-latest
    steps:
      - name: 📥 檢出代碼
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: 🔍 檢查 root 命名
        run: |
          set -euo pipefail
          violations=0
          for file in root.* init.d/* bin/* sbin/* etc/* lib/* var/* usr/*; do
            [ -e "$file" ] || continue
            basename=$(basename "$file")
            case "$basename" in
              *[A-Z]*)
                echo "❌ 文件含有大寫字母: $file" >> "$GITHUB_STEP_SUMMARY"
                violations=$((violations+1))
                ;;
              *\ * )
                echo "❌ 文件含有空白: $file" >> "$GITHUB_STEP_SUMMARY"
                violations=$((violations+1))
                ;;
            esac
          done

          if [ $violations -eq 0 ]; then
            echo "✅ 所有檔案符合命名規範" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "總計 $violations 項命名違規" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
